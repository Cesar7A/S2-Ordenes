spring:

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB_NAME:orders}?createDatabaseIfNotExist=true
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:123456asd}
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: always

  jpa:
    database: MYSQL
    show-sql: false
    hibernate:
      ddl-auto: update

  sql:
    init:
      mode: always
  mail:
    host: smtp.mailtrap.io
    port: 2525
    username: 76c20d7f8d88b1
    password: 8794b14be4b8e9
    properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true
server:
  port: 8081
  error:
    include-binding-errors: always
    include-message: always
    include-exception: true
    include-stacktrace: always